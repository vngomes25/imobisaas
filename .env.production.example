# Copie este arquivo para .env.production e preencha os valores

# Banco de dados
DB_ROOT_PASSWORD=troque_por_senha_forte_root
DB_PASSWORD=troque_por_senha_forte_app

# Segredo JWT — gere com: openssl rand -hex 32
JWT_SECRET=troque_por_string_aleatoria_longa

# (Opcional) Se quiser HTTPS com domínio próprio
# DOMAIN=suaimobiliaria.com.br
